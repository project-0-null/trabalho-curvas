# Teste Computacional - Ajuste de Curvas (Mínimos Quadrados)

Este repositório contém a resolução do Teste Computacional 1 (2025/2) do curso de **Engenharia Elétrica**.

## 📝 Sobre o Trabalho
O objetivo deste projeto é implementar algoritmos para o ajuste de funções não lineares utilizando o **Método dos Mínimos Quadrados** com **linearização**.

Conforme solicitado nas instruções, o código foi desenvolvido em Python implementando manualmente a resolução do sistema linear (Eliminação de Gauss com pivoteamento), se utilizarm bibliotecas prontas de ajuste de curvas (como `polyfit` ou similar).

## 🚀 Problemas Resolvidos
O programa oferece um menu para resolver os dois casos propostos:

1.  **Problema 1 (Circuito RC):** Ajuste de modelo exponencial ($i = ae^{bt}$) para dados de corrente x tempo.
2.  **Problema 2 (Volume de Árvores):** Ajuste de modelo de potência ($v = ax^b$) para dados de volume x diâmetro.

## 🛠️ Como Executar
Instale os requirements.txt (pip install -r requirements.txt), execute o progrma TC1-menu.py
